version: '3'
services:
  nightcrawler:
    build:
      dockerfile: ./dockerfile
      context: .
    image: nightcrawler
    working_dir: /app/phi_nightcrawler
    env_file:
      ./.env_file
    volumes:
      - .:/app/phi_nightcrawler/
      - $HOME:/root